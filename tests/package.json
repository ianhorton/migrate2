{
  "name": "sls-to-cdk-migration-tests",
  "version": "1.0.0",
  "description": "Test suite for Serverless-to-CDK migration tool",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:unit": "jest unit/",
    "test:integration": "jest integration/",
    "test:e2e": "jest integration/e2e-migration.test.ts",
    "test:verbose": "jest --verbose",
    "test:silent": "jest --silent",
    "test:clear-cache": "jest --clearCache"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-logs": "^3.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "setupFilesAfterEnv": ["<rootDir>/setup.ts"]
  }
}
